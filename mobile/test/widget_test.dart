import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App load test', (WidgetTester tester) async {
    // Basic test to avoid build failure from invalid default test
    expect(true, true);
  });
}
